#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int N;
    cin >> N;
    if (N == 0) {
        cout << "divide by zero";
    }
    else {
        for (int i = 0; i < N; i++) {
            int M;
            cin >> M;
        }
        cout << "1.00";
    }

    return 0;
}